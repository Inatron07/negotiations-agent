PORT=3000
ALLOWED_ORIGIN=*
CHATBOT_BASE_URL=https://ai.automationanywhere.com/
CHATBOT_PROJECT_ID=
CHATBOT_AGENT_ID=
CHATBOT_API_KEY=
CHATBOT_API_SECRET=

# Persistent store for the demo credit counter (Render Key Value / Redis
# connection string). Required in production — without it the app falls
# back to an in-memory counter that resets on every restart/spin-down.
CREDIT_STORE_URL=
